json.extract! admin_primary_color, :id, :name, :code, :price, :stocks, :image, :product_id, :color_id, :active, :created_at, :updated_at
json.url admin_primary_color_url(admin_primary_color, format: :json)
